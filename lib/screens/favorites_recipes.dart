import 'package:flutter/material.dart';

class Favorites_Recipes extends StatelessWidget{
  const Favorites_Recipes({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: const Text('Agregar recetas a favoritos'));
  }
}